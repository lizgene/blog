

<div class="row">
  <div class="col-sm-12">
    <h1><%= @post.title %></h1>
  </div>
  <div class="col-sm-12">
    <%= simple_format @post.body %>
  </div>
</div>

<% if user_signed_in? %>
  <%= link_to 'Edit', edit_post_path(@post) %>
<% end %>
<%= link_to 'Back', posts_path %>
