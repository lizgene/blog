//grand budapest
$gb-mauve: #7C4C4D;
$gb-peach: #EC9283;
$gb-pink: #FA6D7A;
$gb-light-mauve: #C08A85;
$gb-navy: #534659;
$gb-teal: #487C87;

//life acquatic palette
$acquatic-light-yellow: #F3F13A;
$acquatic-dark-yellow: #D79C00;
$acquatic-light-blue: #68ABB8;
$acquatic-dark-blue: #3188A3;
$acquatic-red: #E50000;
$acquatic-light-gray: #C3CED0;
$acquatic-skin-color: #E5C39E;

//fantastic mr fox
$fox-white: #E7D2B7;
$fox-yellow: #E0C800;
$fox-blue: #3E98BB;
$fox-orange: #DA7300;
$fox-red: #A40018;
$fox-burnt-orange: #D07317;

//moonrise kingdom
$mk-light-blue: #DAECEE;
$mk-red: #97000E;
$mk-yellow: #E8B300;
$mk-gold: #9A711E;
$mk-blue: #7BB4A5;
$mk-white: #E2CDA9;

//universal variables

$dark-base: $acquatic-dark-blue;
$white-base: $acquatic-light-gray;
$light-base: $acquatic-skin-color;
$bold-base: $acquatic-red;
$bright-base: $acquatic-light-blue;
$highlight-base: $acquatic-light-yellow;

//Wes Anderson Fonts
$acquatic-headings-font-family: 'Futura BQ';
$mk-headings-font-family: $font-family-cursive;
$wes-anderson-headings-font-family: 'Futura';

html#wedding {
  width: 100%;
  height: 100%;

  body {
      width: 100%;
      height: 100%;
  }

  .full-page {
    padding-top: 100px; //height of navbar is 90px
    padding-bottom: 50px;
    min-height: 100%;
    background-size: cover;
    background-attachment: fixed;
  }

  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color:rgba(25, 25, 25, 0.4);
  }

  .headshot {
    max-height: 300px;
  }
  //this is just a helper for colors

  .palette {
    li:nth-child(1) > i:first-child {
      color: $white-base;
    }
    li:nth-child(2) > i:first-child {
      color: $light-base;
    }
    li:nth-child(3) > i:first-child {
      color: $highlight-base;
    }
    li:nth-child(4) > i:first-child {
      color: $bold-base;
    }
    li:nth-child(5) > i:first-child {
      color: $bright-base;
    }
    li:nth-child(6) > i:first-child {
      color: $dark-base;
    }
  }
  
  .intro-section {
    color: $highlight-base;
    font-weight: bold;
    text-align: center;
    h1, h2 {
      margin: 0;
      font-stretch: ultra-condensed;
      text-align: center;
      font-family: $acquatic-headings-font-family;
    }
    h1 {
      font-size: 94px;
    }
    padding-top: 150px;
    font-family: $wes-anderson-headings-font-family;
    @extend .full-page;
    background-image: url(<%= asset_path 'wedding/cups_bw.jpg' %>);
  }

  .cast-section {
    // background: #f4f4f2 url(<%= asset_path 'wedding/teal_texture.jpg' %>) 0 0 repeat;
    background-color: $white-base;
    @extend .full-page;

    .credits {
      font-family: $wes-anderson-headings-font-family !important;
      color: $bold-base;
    }
  }

  .story-section {
    background-image: url(<%= asset_path 'wedding/handhold_bw.jpg' %>);
    // background: #f4f4f2 url(<%= asset_path 'wedding/brownpaper.jpg' %>) 0 0 repeat;
    background-color: $dark-base;
    color: $gray-darker;
    font-family: $font-family-monospace;
    @extend .full-page;

    h4 {
      font-family: $font-family-monospace;
    }
    .chapter {
      @extend .shadow-box;
      color: white;
      height: 350px;
      max-width: 700px;
      margin: 0 auto;
      padding: 20px;
    }

    .carousel-control {
      background: none;
      .glyphicon {
        margin-top: -150px;
      }
      &.left {
        left: -15%;
      }
      &.right {
        right: -15%;
      }
    }
    .carousel-indicator-styles {
      display: inline-block;
      border: none;
      width: 35px;
      height: 35px;

      text-indent: 0;
      cursor: pointer;
      background-color: transparent;
      margin: 0;
    }
    .carousel-indicators li {
      @extend .carousel-indicator-styles;

      .fa-stack-2x { //bottom layer (circle)
        color: $bright-base;
      }
      .fa-stack-1x { //top layer (flag or whatever)
        color: $highlight-base;
      }
    }
    .carousel-indicators li.active {
      @extend .carousel-indicator-styles;

      .fa-stack-2x { //bottom layer
        color: $highlight-base;
      }
      .fa-stack-1x { //top layer
        color: $bright-base;
      }
    }

  }

  .when-and-where {
    font-family: $wes-anderson-headings-font-family;
    // background-image: url(<%= asset_path 'wedding/handhold_bw.jpg' %>);
    background-color: $bold-base;
    @extend .full-page;

    .invitation {
      @extend .shadow-box;
      border: 5px double $highlight-base;
      max-width: 700px;
      padding: 20px;
      color: $highlight-base;

      h1 {
        font-size: 72px;
      }
    }

  }

  .guide {
    color: $highlight-base;
    background-image: url(<%= asset_path 'wedding/window_handholds.jpg' %>);
    @extend .full-page;

    h1 {
      font-size: 58px;
    }

    #filters {
      background-color:rgba(25, 25, 25, 0.5);
      padding: 20px;
      background-clip: padding-box;
      border: 10px solid transparent;
      text-transform: uppercase;
      font-family: $wes-anderson-headings-font-family;
      font-size: 24px;
      ol {
        margin: 0;
        li {
          margin-left: 5px;
          margin-right: 5px;
          padding-bottom: 5px;
          border-bottom: 3px solid transparent;
          cursor: pointer;
          &:hover {
            border-bottom: 3px solid $bold-base;
          }
        }
      }
    }

    .place {
      a {
        color: $white-base;
        text-decoration: none;
      }
      background-color: $dark-base;
      color: $white-base;
      background-clip: padding-box;
      border: 10px solid transparent;

      p {
        font-size: 14px;
        text-transform: uppercase;
        font-family: $wes-anderson-headings-font-family;
      }

      h2 {
        font-family: $acquatic-headings-font-family;
        border-top: 1px solid $bright-base;
        border-bottom: 1px solid $bright-base;
        margin-top: 10px;
        margin-bottom: 10px;
        padding: 10px;
        text-transform: uppercase;
      }

      &:hover {
        background-color: lighten($dark-base, 3%);
        -webkit-transition: background-color 100ms linear;
        -moz-transition: background-color 100ms linear;
        -o-transition: background-color 100ms linear;
        -ms-transition: background-color 100ms linear;
        transition: background-color 100ms linear;
      }
    }
  }

  .rsvp-section {
    background-color: darken($white-base, 5%);
    text-align: center;

    .header {
      h1 {
        font-family: $wes-anderson-headings-font-family;
        color: $highlight-base;
        text-transform: uppercase;
        font-size: 72px;
      }
      h2 {
        color: $highlight-base;
        text-transform: uppercase;
        padding: 0;
        margin: 0;
        font-size: 24px;
      }
    }
    .rsvp-buttons {
      a {
        text-decoration: none;
        border-radius: 50%;
        color: $bright-base;
        font-size: 72px;
        text-transform: uppercase;
        font-family: $wes-anderson-headings-font-family;
        padding: 15px;
        &:hover {
          border: 5px solid $bright-base;
        }
      }
    }

    form {
      font-family: $wes-anderson-headings-font-family;
      h2 {
        color: $highlight-base;
      }
      input[type="submit"] {
        background-color: $bright-base;
        border: none;
        text-transform: uppercase;
        &:hover {
          background-color: lighten($bright-base, 5%);
        }
      }
    }
    // background-image: url(<%= asset_path 'wedding/moonrisekingdom-married.jpg' %>);
    @extend .full-page;
  }

  .faq {
    background: url(<%= asset_path 'wedding/bananas.jpeg' %>) 0 0 repeat;
    
    .panel {
      font-family: $wes-anderson-headings-font-family;
      border: none;
    }
    .panel-heading {
      text-transform: uppercase;
      color: $highlight-base;
      background-color: $bright-base;
      a {
        text-decoration: none;
      }
    }

    p {
      font-family: $font-family-monospace;
    }

    @extend .full-page;
  }

  .quote-section {
    position: relative;
    height: 5px;
    text-align: center;
    color: $white-base;
    background-color: $dark-base;
    // background: $mk-gold url(<%= asset_path 'wedding/gold-border.jpg' %>) 0 0 repeat;  
  }


  // Navbar

  nav {
    li {
      a {
        font-size: 24px;
        font-family: $headings-font-family;
      }
    }
  }

  #wedding-header {
    height: 80px;
    margin: 0;
    position: absolute;
    width: 100%;
    bottom: 0;
    background-color:rgba(25, 25, 25, 0.4);

    &:after {
      background: transparent url(<%= asset_path 'wedding/diagonal_texture.png' %>) 0 0 repeat;
      content: '';
      display: block;
      width: 100%;
      height: 24px;
      position: absolute;
      bottom: -10px;
    }
  }

  #wedding-header.affix {
    position: fixed;
    background-color:rgba(25, 25, 25, 0.4);
    top: 0;
    width: 100%;
    z-index: 100;
    left: 0;
    margin-top: 0;

  }

  .navbar .nav,
  .navbar .nav > li {
    float:none;
    display:inline-block;
    *display:inline; /* ie7 fix */
    *zoom:1; /* hasLayout ie7 trigger */
    vertical-align: top;
  }

  .navbar-inner {
    text-align:center;
  }

  .nav {
   li a {
      color: $white-base;
      padding: 0px 0px 10px 0px;
      margin: 0px 15px;
      &:link, &:hover, &:visited, &:active {
        background-color: transparent;
        outline: 0;
        -webkit-tap-highlight-color: rgba(0, 0, 0, 0); //gets rid of grey background on link when clicked
      }
    }
  }

  .navbar-link {
    min-width: 45px;
  }

  .navbar-logo {
    height: 2em;
    margin-top: -5px;
  }


  @media(min-width:767px) {
    .navbar {
        padding: 20px 0;
        -webkit-transition: background .5s ease-in-out,padding .5s ease-in-out;
        -moz-transition: background .5s ease-in-out,padding .5s ease-in-out;
        transition: background .5s ease-in-out,padding .5s ease-in-out;
    }

    .top-nav-collapse {
        padding: 0;
    }
  }

  // utilities

  .text-headers-small {
    font-size: 32px !important;
  }

  .vertical-center {
    padding: 0;
    min-height: 100%;  /* Fallback for browsers do NOT support vh unit */
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .shadow-box { //essentially a background box with low opacity
    background-color:rgba(25, 25, 25, 0.5);
  }

  .futura {
    font-family: $wes-anderson-headings-font-family !important;
  }

  .cursive {
    font-family: $font-family-cursive;
  }

  .grayscale {
    -webkit-filter: grayscale(100%);
  }
  .sepia {
    -webkit-filter: sepia(100%);
  }
}