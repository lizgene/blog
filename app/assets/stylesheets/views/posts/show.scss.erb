@import "bootstrap/variables";

h1 {
  font-size: 18px;
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: bold;
}
.header {
  .date {
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
    font-weight: bold;
  }
}

.body {
  padding-top: 20px;
  padding-bottom: 20px;
}