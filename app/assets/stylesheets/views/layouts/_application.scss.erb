*, *:before, *:after {
    -webkit-box-sizing: border-box; /* Needed for mobile WebKit */
    -moz-box-sizing: border-box; /* Needed for Firefox */
    box-sizing: border-box
}

html, body {
  background-color: white;
}
main {
  margin-top: 100px;
}

footer {
  height: 100px;
  margin-top: 50px;
  padding: 20px 0px;
}
.footer-container {
  display: table;
  margin: 0 auto;
  height: 100%;
  position: relative;
  border-top: 1px solid $gray-lighter;
}
.footer-content {
  display: table-cell;
  position: relative;
  text-align: center;
  vertical-align: middle;
}

a {
  cursor: pointer;
}

h1, h2 {
  padding-bottom: 5px;
  a {
    padding-bottom: 5px;
    &:hover {
      text-decoration: none;
      border-bottom: 2px solid $brand-danger; 
    }
  }
}

.container {
  max-width: 100%;
}
a.back {
  font-size: 12px;
  text-transform: lowercase;
}



    .pin {
      display: inline-block;
      background: #FEFEFE;
      margin: 0;
      padding: 15px;
      padding-bottom: 5px;
      background: -webkit-linear-gradient(#FFF, #F9F9F9);
      opacity: 1;
      
      -webkit-transition: all .2s ease;
      -moz-transition: all .2s ease;
      -o-transition: all .2s ease;
      transition: all .2s ease;
      &:hover {
        opacity: 0.8;
        cursor: pointer;
      }
    }

  .pin .preview-image {
    height: 300px;
    position: relative !important;
    transition: all 200ms;
    padding: 0;
    color: white;
    background-position: top center;
    background-repeat: no-repeat;
    background-size: cover;
    padding-bottom: 15px;
    border-bottom: 1px solid $gray;
    margin-bottom: 20px;
    &:before {
      content: "";
      display:  block;
      position: absolute;
      top: 0;
      right: 0;
      bottom: 0;
      left: 0;
      pointer-events: none;
      background: rgba(0,0,0,0.35);
    }
    h2 {
      color: white;
      font-size: 18px;
      font-weight: bold;
      position: absolute;
      bottom: 15px;
      padding-left: 15px;
      padding-right: 15px;
      white-space:nowrap;
      text-overflow:ellipsis;
      overflow: hidden;
      width: 100%;
    }
    p {
      color: white;
      font-size: 12px;
      font-weight: bold;
      position: absolute;
      bottom: 5px;
      padding-left: 15px;
      padding-right: 15px;
      white-space:nowrap;
      text-overflow:ellipsis;
      overflow: hidden;
      width: 100%;
    }
  }

  .pin p {
    font: 14px/18px Helvetica, sans-serif;
    color: #333;
    line-height: 1.5em;
    margin: 0;
    height: 5em;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .clickable {
    cursor: pointer;
  }

  .col-pad-all { //col-x-x only puts padding on left and right. add padding all around.
    padding: 15px;
  }

  .centered-axis-x {
    position: absolute;
    left: 50%;
    @include translate(-50%, 0);
  }

  .centered-axis-xy {
    position: absolute;
    left: 50%;
    top: 50%;
    @include translate(-50%, -50%);
  }


  //for the image zoom containers
  .image-zoom img {
    @include transition(all 1.25s ease);
    width: 100%;
    @media(max-width: $max-tablet-width){
      opacity: 0.3;
    }
  }
  .image-zoom h2 {
    opacity: 0;
    z-index: 2;
    text-transform: uppercase;
    color: $gray-dark;
    width: 80%;
    font-weight: bold;
    @media(max-width: $max-tablet-width){
      opacity: 1;
    }
  }

  .image-zoom:hover img {
    @include scale(1.1);
    opacity: 0.3;
  }
  .image-zoom:hover h2 {
    opacity: 1;
  }

  .image-zoom {
    width:100%;
    height:450px;
    overflow: hidden;
    @media(max-width: $max-tablet-width){
      opacity: 1;
      height: auto;
      max-height:450px;
    }
  }
