@import "variables";

.image-lightbox-modal button.next-right {
  background-color: transparent;
  border: none;
  color: white;
  font-size: 72px;
  position: absolute;
  top: 40%;
  right: -35px;
  &:focus {
    outline: none;
  }
}
