require 'rails_helper'

RSpec.describe User, type: :model do
  pending "has a valid factory"
  pending "fails validation if email is not present"
  pending "fails validation if password is not present"
  pending "successfully logs in with Google OAuth"
  pending "successfully logs in without Google OAuth"
end
